export type User = {
    userId: number,
    name: string,
    email: string,
    role: string,
    accessToken: string,
    refreshToken: string,
    loginTime: string,
    profilePictureUrl: string,
}